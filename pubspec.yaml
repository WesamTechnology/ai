name: app_ai
description: "A complete and professional AI Chat Application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.2
  
  # UI/Text Rendering
  flutter_markdown: ^0.7.2
  google_fonts: ^6.2.1
  
  # Networking (For Free AI API)
  http: ^1.2.0
  
  # Utilities
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
